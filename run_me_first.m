% Add folders to path.

% NB: to run the code, please go to
% https://github.com/NicolasBoumal/manopt to download the folder and place
% in the directory.

addpath(pwd);

cd manopt;
addpath(genpath(pwd));
cd ..;

cd factories;
addpath(genpath(pwd));
cd ..;

cd proposed;
addpath(genpath(pwd));
cd ..;


cd baselines;
addpath(genpath(pwd));
cd ..;


